<h2>packs</h2>
<table cellspacing='0px'>
	<thead>
	<tr>
		<% for(var attr in Graoupack.Models.Pack.attributes){%>
			<% if(attr == 'id') continue;%>
			<th><%= attr%> </th>	 
		<%}%>
		<th>Options</th>
	</tr>
	</thead>
	<tbody>
		<%= $.View('//graoupack/views/pack/list',{packs: packs})%>
	</tbody>
</table>
<h2>New pack</h2>
<form>
	<% for(var attr in Graoupack.Models.Pack.attributes){ %>
		<% if(attr == 'id') continue;%>
		<div>
			<label><%= attr %> </label><br />
			<input type="text" value="" name="<%= attr %>"/>
		</div>
	<%}%>
	<input type='submit' value='Create'/>
</form>